<Window x:Class="Modultask2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Modultask2"
        mc:Ignorable="d"
        Title="Обробка даних працівників" Height="500" Width="750"
        Background="{StaticResource BackgroundBrush}"
        MinWidth="600" MinHeight="400">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Grid.Row="0" Text="Обробка даних працівників" 
                   Style="{StaticResource HeaderText}" Margin="0,0,0,10"/>

        <!-- Кнопки та поріг -->
        <StackPanel Grid.Row="1" Orientation="Vertical" HorizontalAlignment="Center" Margin="0,0,0,10">
            <WrapPanel HorizontalAlignment="Center">
                <Button Name="BtnGenerate" Click="BtnGenerate_Click">Згенерувати</Button>
                <Button Name="BtnLoad" Click="BtnLoad_Click">З файлів</Button>
                <Button Name="BtnRun" Click="BtnRun_Click">Запити</Button>
                <Button Name="BtnSave" Click="BtnSave_Click">Зберегти</Button>
                <Button Name="BtnExit" Click="BtnExit_Click">Вийти</Button>
            </WrapPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,6,0,0">
                <TextBlock Text="Поріг річної зарплати:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <TextBox Name="TxtThreshold" Width="100" Background="White"/>
            </StackPanel>
        </StackPanel>

        <!-- Результати -->
        <TextBox Grid.Row="2" Name="TxtResults" IsReadOnly="True" TextWrapping="Wrap" AcceptsReturn="True" 
                 VerticalScrollBarVisibility="Auto" Margin="0,0,0,10" Background="White" Foreground="{StaticResource TextBrush}"/>

        <!-- Повідомлення -->
        <ListBox Grid.Row="3" Name="ErrorsList" Height="80" HorizontalContentAlignment="Stretch"/>
    </Grid>
</Window>