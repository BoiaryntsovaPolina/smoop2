<Window x:Class="StoreApp.Views.ProductEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Картка товару" Width="500" 
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">

    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#444444"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="90"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition x:Name="RowLocation" Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Height="28" VerticalAlignment="Top">
            <TextBlock Text="Назва:"/>
        </Border>
        <StackPanel Grid.Row="0" Grid.Column="1" Margin="0,0,0,12">
            <TextBox Name="TxtName" Height="28" VerticalContentAlignment="Center"/>
            <TextBlock Name="ErrName" Style="{StaticResource ErrorTextStyle}"/>
        </StackPanel>

        <Border Grid.Row="1" Height="28" VerticalAlignment="Top">
            <TextBlock Text="Категорія:"/>
        </Border>
        <StackPanel Grid.Row="1" Grid.Column="1" Margin="0,0,0,12">
            <ComboBox Name="CbCategory" Height="28" VerticalContentAlignment="Center"/>
            <TextBlock Name="ErrCategory" Style="{StaticResource ErrorTextStyle}"/>
        </StackPanel>

        <Border Grid.Row="2" Height="28" VerticalAlignment="Top">
            <TextBlock Text="Кількість:"/>
        </Border>
        <Grid Grid.Row="2" Grid.Column="1" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="90"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <ComboBox Name="CbQuantity" Height="28" VerticalContentAlignment="Center" 
                          IsEditable="True" PreviewTextInput="TxtNumbersOnly_PreviewTextInput"/>
                <TextBlock Name="ErrQuantity" Style="{StaticResource ErrorTextStyle}"/>
            </StackPanel>

            <ComboBox Grid.Column="2" Name="CbUnit" Height="28" VerticalAlignment="Top" VerticalContentAlignment="Center"/>
        </Grid>

        <Border Grid.Row="3" Height="28" VerticalAlignment="Top">
            <TextBlock Text="Ціна (грн):"/>
        </Border>
        <StackPanel Grid.Row="3" Grid.Column="1" Margin="0,0,0,12">
            <TextBox Name="TxtPrice" Height="28" VerticalContentAlignment="Center" PreviewTextInput="TxtPrice_PreviewTextInput"/>
            <TextBlock Name="ErrPrice" Style="{StaticResource ErrorTextStyle}"/>
        </StackPanel>

        <Border x:Name="LblLocation" Grid.Row="4" Height="28" VerticalAlignment="Top">
            <TextBlock Text="Місце:"/>
        </Border>
        <StackPanel Grid.Row="4" Grid.Column="1" Margin="0,0,0,12">
            <TextBox Name="TxtLocation" Height="28" VerticalContentAlignment="Center" ToolTip="Наприклад: Гараж, Коробка №2"/>
            <TextBlock Name="ErrLocation" Style="{StaticResource ErrorTextStyle}"/>
        </StackPanel>

        <StackPanel Grid.Row="5" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,15,0,5">
            <Button Content="Зберегти" Click="Ok_Click" Background="#4CAF50" Foreground="White" Width="130" Height="35"/>
            <Button Content="Скасувати" Click="Cancel_Click" Width="110" Height="35" Margin="15,0,0,0"/>
        </StackPanel>
    </Grid>
</Window>